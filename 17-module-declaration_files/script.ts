import { slide } from "./cafu.js";

console.log(slide('cafu'))

const wolfpack: Hallowed = {
  nome: 'shore',
  preco: 1945,
}

declare global {
  interface UnderFire {
    nome: string;
    preco: number;
  }
}
